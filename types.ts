
export interface GenerationResult {
  imageUrl: string;
  prompt: string;
}

export interface ThumbnailSettings {
  title: string;
  subtitle: string;
  textColor: string;
  overlayOpacity: number;
  showRainText: boolean;
}
